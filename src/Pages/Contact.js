const Contact = ({scroll}) => {
    return (
      <div ref={scroll} style={{height: '100vh', padding: '5em', background: 'blue'}}>
        <h1>Hello this is Contact</h1>
      </div>
    );
  };
  export default Contact;